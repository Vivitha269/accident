# Local environment variables (not committed to GitHub)
# Copy this file and rename to .env, then fill in your values

# Firebase credentials as JSON string (copy the entire content from ai-accident-firebase-adminsdk-fbsvc-0b4a184229.json)
FIREBASE_CREDENTIALS={"type": "service_account", "project_id": "ai-accident", ...}  # Replace with full JSON

TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number